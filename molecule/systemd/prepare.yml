---
- name: Prepare | Install picodata (systemd, non-rootless)
  hosts: all
  gather_facts: true
  become: true
  tasks:
    - name: Install picodata package
      ansible.builtin.include_role:
        name: picodata-ansible
        tasks_from: install_packages.yml
      vars:
        install_packages: true
